{
  "name": "example_echo",
  "version": "0.1.0",
  "description": "Developer-only example tool used to verify Moxue's external tool gateway.",
  "enabled": true,
  "entrypoint": "tool.py",
  "trigger_keywords": ["工具測試", "外接工具測試", "echo tool"],
  "always_available": false,
  "actions": {
    "echo": {
      "description": "Echo text through the external tool gateway. Use only when the user asks to test the external tool system.",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "Text to echo back."
          }
        },
        "required": ["text"]
      }
    },
    "status": {
      "description": "Return basic gateway context for an external tool system test.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  }
}
